@model IEnumerable<WebApplication1.Amphbian>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Animal)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SurviveOnLand)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Pop)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Dietry)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Animal)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SurviveOnLand)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Pop)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Dietry)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AmphbiansID }) |
            @Html.ActionLink("Details", "Details", new { id=item.AmphbiansID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AmphbiansID })
        </td>
    </tr>
}

</table>
